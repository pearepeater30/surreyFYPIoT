These lines of code need to be run on a LoPy4 Node.

To run these, you will need to use the Atom IDE and have the PyMakr Plugin. 
Open the either the LoRa Gateway folder or the LoRa Node folder.
Connect the LoPy board to the computer. 
Then upload the code to the LoPy board.
